# Enhanced Calculator

A command-line calculator with history, undo/redo, observers (logging + autosave), and CI.

## Setup

```bash
python -m venv venv
source venv/bin/activate  # or venv\Scripts\activate
pip install -r requirements.txt
